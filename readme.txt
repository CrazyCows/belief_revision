How to run our belief_revision:

Install Python.

Open your python IDE.

Make a virtual environment using the following:
py -m venv venv

Go into your virtual environment:
venv\Scripts\activate

Install the requirements from requirements.txt using:
pip install -r requirements.txt

Now you can run the AI using the following command or use the main function in the follwing filename after py:
py main.py

